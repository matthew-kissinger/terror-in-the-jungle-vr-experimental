import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  base: '/terror-in-the-jungle-vr-experimental/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  }
})